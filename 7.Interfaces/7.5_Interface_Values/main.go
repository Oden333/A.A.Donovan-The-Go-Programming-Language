package main

func main() {

}

//! Conceptually, a value of an interface type, or interface value, has two components, a
//! concrete type and a value of that type.
//
//! These are called the interface’s dynamic type and dynamic value.

//? In Go, types are a compile-time concept, so a type is not a value.
